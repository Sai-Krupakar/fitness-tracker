import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.levelup.trainingarc',
  appName: 'Level Up Training Arc',
  webDir: 'dist',
}

export default config
